{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clear": "rimraf ./dist",
    "compy-index-html": "copyfiles -f ./src/index.html ./dist",
    "build": "npm run clear && npm run compy-index-html && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "watch": {
    "build": {
      "patterns": [
        "src"
      ],
      "extensions": "ts"
    }
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "reflect-metadata": "^0.1.13",
    "requirejs": "^2.3.6",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "@types/jsonwebtoken": "^8.5.1",
    "jsonwebtoken": "^8.5.1",
    "node-fetch": "^2.6.1",
    "npm-watch": "^0.7.0",
    "rimraf": "^3.0.2",
    "tsc": "^6.14.11.0"
  }
}
